# cal.dudu-works.com 기본 실습 앱

버전 1.0, 2026-09-07. cal.dudu-works.com은 가상의 서비스명이며 실제 도메인 연결·배포 완료를 뜻하지 않습니다. Cal.com 형태의 예약 도구를 참고한 무디자인 기본 앱입니다. Cal.com의 전체 기능·소스·브랜드를 복제하지 않습니다.

## 기본값은 모든 실습자에게 같습니다

- 한국 시간 2026-09-09~2026-09-22 양끝 포함, 주말 포함 14일. 실습일 9월 7~8일과 예약 대상 날짜를 구분합니다.
- 매일 오전·오후·저녁, 총 42슬롯. 하나의 슬롯에 확정 고객 한 명.
- 오전 09:00, 오후 13:00, 저녁 18:00 시작. 날짜와 슬롯 라벨로만 예약합니다. 지역별 소요시간·연속 슬롯 계산은 없습니다.
- 실행 시 현재 시각을 사용합니다. Supabase는 서버 시각, 로컬 미리보기는 기기 시각을 사용하며 시작 시각이 지난 슬롯은 신청·확정할 수 없습니다. 자동 테스트에서만 시각을 고정해 같은 사례를 재현합니다.
- 고객은 서로 다른 열린 슬롯을 1~3개 선택합니다. 선택 순서가 희망 1·2·3순위입니다.
- 접수는 점유가 아닙니다. 여러 고객이 같은 열린 슬롯에 신청할 수 있습니다.
- 확정만 슬롯을 마감합니다. 어드민이 고객의 남은 희망 중 하나를 수동 확정합니다.
- 조회 후 마감된 슬롯이 있으면 제출 전체를 거절하고 다시 선택하게 합니다. 조용히 후보를 삭제하거나 순위를 바꾸지 않습니다.
- 모든 희망이 소진되면 재선택 필요. 다른 슬롯을 임의 배정하지 않습니다.
- 고객당 신청 하나. 재선택 필요 상태에서만 후보를 새 버전으로 재제출합니다. 이전 후보 이력은 보존하고 새 접수 순번을 부여합니다.
- 초기 상태는 슬롯 42개, 신청·확정 0개입니다. 별도 데모 데이터 로드는 명시적 실습 동작입니다.
- 기본은 수동 확정만입니다. 자동 확정·대기 수 제한·알림·RAG·Agent·결제·취소는 기본에 없습니다.

## 고객 화면

서비스명, 실습 시각, 접속 모드, 날짜 14행×시간 3열의 표를 표시합니다. 열린 슬롯만 선택 가능하고 마감 슬롯은 비활성화합니다. 선택한 희망 순위를 별도 목록에 보여주고 제거 후 재선택할 수 있습니다. 제출 전 최종 확인을 거쳐 저장합니다.

내 신청에서 원래 희망·현재 가능 여부·현재 상태·확정 시간을 확인합니다. 모든 후보가 소진되면 재선택 화면을 제공합니다. 새로고침 후 실제 저장 결과가 유지돼야 합니다.

## 어드민 화면

접수 순서 목록, 고객 코드, 희망 순위, 후보별 가능 여부, 수동 확정 버튼을 제공합니다. 원래 희망 밖 슬롯은 선택할 수 없습니다. 확정 후 슬롯 마감과 영향받은 다른 고객의 재선택 상태를 다시 표시합니다.

실행 기록에는 작업 ID, 요청 ID, 행위, 결과, 오류 위치를 표로 제공합니다. 입력 검사·현재 상태 조회·확정 저장 실패를 구분합니다. 저장 실패를 성공이나 후보 소진으로 바꾸지 않습니다.

## 고정 판정

날짜 같음 O/X와 시간 라벨 같음 O/X의 네 조합 중 O/O만 동일 슬롯입니다. 동일한 대기 후보는 허용하고 동일한 확정 슬롯은 금지합니다.

상태는 received(접수), needs_reselection(재선택 필요), confirmed(확정) 세 가지입니다. 조회·제출·확정 시 현재 시각과 점유를 다시 확인합니다. 미확정 신청의 모든 후보가 마감되거나 시작 시각이 지나면 재선택 필요로 표시합니다. 전체 예약 기간이 끝나면 기간 종료를 표시하고 새 선택을 막습니다. 확정 이력과 원래 희망 순위는 보존합니다.

## 데이터·권한·저장 규약

슬롯, 신청, 희망 후보, 확정, 실행 기록을 구조화합니다. 희망 후보는 문자열 합치기로 저장하지 않습니다. 요청당 확정 한 건과 슬롯당 확정 한 건을 DB 제약으로 보장합니다. 한 확정의 예약 저장·상태 변경·영향 신청 갱신은 하나의 트랜잭션입니다.

Supabase 모드에서 고객은 로그인한 자기 신청만 읽고 제출하며 어드민만 전체 요청을 보고 확정합니다. user_metadata를 권한으로 믿지 않습니다. 브라우저에는 공개 가능한 URL과 publishable/anon key만 사용합니다. service_role·DB password·LLM key는 제공 파일과 브라우저에 넣지 않습니다. 읽기 실패 시 로컬 데이터로 자동 대체하지 않습니다.

기본 로컬 미리보기는 별도 자격증명 없이 같은 초기값으로 실행하는 수업용 모드입니다. 역할 전환은 이 모드에만 있고 진짜 인증이라고 표시하지 않습니다. Supabase 모드에서는 실제 인증·DB 권한이 적용됩니다. 로컬 미리보기와 Supabase는 동일한 업무 사례를 통과해야 하며, 모드가 화면에 명확히 표시됩니다.

## 디자인 범위

기본 HTML 폼·버튼·표·제목·오류 텍스트만 사용합니다. 장식·아이콘·이미지·애니메이션·Tailwind·차트·디자인 시스템은 추가하지 않습니다. 모바일에서도 표가 가로 스크롤되어 항목이 사라지지 않게 합니다. 라벨·키보드 조작·로딩·오류 표시는 유지합니다.

## 결과 확인 사례

1. 처음 실행하면 42슬롯과 신청 0개가 보입니다.
2. C01이 9/9 오전·오후, C02가 9/9 오전을 신청하면 둘 다 접수됩니다.
3. 어드민이 C01의 9/9 오전을 확정하면 C02는 재선택 필요입니다.
4. C02가 9/10 오전을 다시 선택하면 새 순번으로 접수되고 이전 선택 이력을 확인할 수 있습니다.
5. 같은 슬롯 또는 같은 고객을 동시에 두 번 확정하면 하나만 성공합니다.
6. 동일 작업 ID 재전송은 중복 확정을 만들지 않습니다.
7. 고객이 어드민 확정 기능을 직접 호출하면 거절합니다.
8. 0개·4개·중복·기간 밖·마감 후보 제출을 거절합니다.
9. 저장 실패를 만들면 오류를 표시하고 확정 상태를 만들지 않습니다.
10. 자동 검사 시각을 9/9 09:00 KST로 설정하면 9/9 오전을 신청·확정할 수 없습니다. 이 고정 시각은 테스트에만 주입합니다.

현재 제작 상태: 위 내용은 확정된 요구사항입니다. 날짜 변경을 코드·SQL·데이터에 반영하고 설치·검사를 마친 영수증이 나오기 전에는 배포 완료로 읽지 않습니다.

## 제공 파일

AGENTS.md, CLAUDE.md, PRD.md, START_HERE.md, .env.example, 고정 package-lock.json, React·Vite·TypeScript 코드, SQL 설치 파일과 검사 SQL, 실행 가능한 테스트, 기본값 manifest, 실습 프롬프트, 검수 결과를 같은 폴더에 제공합니다. 파일의 업무 규칙을 프롬프트마다 다시 생성하거나 임의 변경하지 않습니다.

사용자 추가 확정: Supabase SQL, 14일의 일정과 오전·오후·저녁 42슬롯 데이터, 중복확인 로직은 완성된 고정 파일로 ZIP 프로젝트에 포함합니다. 학생 AI의 생성 과제가 아닙니다. 고정 파일 해시를 확인한 후 제공 코드를 실행합니다. ZIP을 새 디렉터리에 풀어 파일 무결성과 동일 기본값을 검사합니다. 실제 비밀키, node_modules, 개인 데이터는 ZIP에 넣지 않습니다.
