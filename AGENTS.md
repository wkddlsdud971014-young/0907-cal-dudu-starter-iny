# cal.dudu 기본 코드 작업 규칙

PRD.md를 먼저 읽으세요. 모든 학생의 첫 실행이 같아야 합니다. 이 폴더만 수정하세요. 다른 세션과 저장소의 파일을 되돌리거나 커밋하지 마세요.

## 구현 규칙

- React·Vite·TypeScript·Supabase. 예약 날짜 2026-09-09~22, 오전·오후·저녁, 42슬롯. 수업일은 9/7~8입니다. 실행 시 현재 시각으로 지난 슬롯을 막고, 자동 검사에서만 시각을 고정합니다. 날짜는 OS 시간대와 무관한 한국 날짜로 해석합니다.
- 1~3개 희망, 대기는 비점유, 확정만 점유. 어드민 수동 확정. 전 후보 소진 시 재선택. PRD의 기본값을 바꾸지 않습니다.
- SQL과 로컬 미리보기의 업무 결과를 같은 시나리오로 검사하세요. demo는 로컬 단일 브라우저 수업용이며 서버 보안·다중 사용자 동시성을 보장한다고 말하지 마세요.
- Supabase는 별도 명시 모드입니다. 환경 변수 누락·연결 오류를 demo 성공으로 숨기지 마세요.
- 직접 테이블 쓰기로 확정 규칙을 우회하지 못하게 SQL 권한을 닫으세요. RPC에서 auth.uid, 어드민, 소유자, 현재 버전, 후보 포함, 점유를 검증하세요. public RPC는 SECURITY INVOKER wrapper, 필요한 privileged 내부 함수는 private schema + 빈 search_path + 명시적 권한 검사·EXECUTE revoke/grant를 사용하세요.
- 슬롯 가용성 공개 조회에는 고객 식별 정보를 포함하지 마세요. 어드민 목록과 고객 자기 신청의 응답을 분리하세요.
- 새 SQL은 실행 순서가 고정된 sql/01_install.sql 등 설치 스크립트로 제공하세요. 기존 운영 DB를 변경하지 마세요. migration을 만들 경우 Supabase CLI 규칙을 따르세요.
- SQL 재실행은 기존 예약을 삭제하지 않으며 42슬롯을 중복 생성하지 않아야 합니다. 설치 SQL과 데이터 초기화를 분리하세요.
- 로그·화면의 성공은 실제 저장 성공 뒤에만 표시합니다. 안정적인 operation ID를 재시도에 사용하세요.
- CSS는 가독성 최소한만. UI 꾸미기·추가 프레임워크·LLM·LangGraph·Edge Function·알림은 기본 범위 밖입니다.
- 모든 npm 의존성은 정확한 버전으로 고정하고 lockfile을 제공합니다. npm ci, npm test, npm run build가 OS와 관계없이 동작해야 합니다.
- Node 권장 버전·환경 변수 이름·첫 실행 프롬프트·실제 로그인 설정과 admin 지정 절차를 START_HERE에 빠짐없이 씁니다. 비밀값이나 실제 계정을 파일에 넣지 않습니다.
- 테스트 기대값은 구현 결과를 베끼지 않고 PRD의 업무 사례로 고정합니다. 실패 테스트를 삭제해서 통과시키지 마세요.

## 수정 전후

Supabase SQL, 14일 42슬롯 데이터, 중복확인 로직은 ZIP에 완성된 상태로 제공하는 보호 대상입니다. 기본 실습에서 수정·재생성하지 마세요. 검사 실패 때 해시 파일을 갱신해 통과시키지 말고 배포된 원본과 차이를 보고하세요. 제작 중 발견한 결함은 감독 검수와 회귀 검사 후 배포 기준선에 반영하며, 배포 이후 변경은 별도 명시 지시를 따릅니다.

기본 설정과 보호된 업무 규칙 파일을 명시하고 SHA256 manifest 검사 명령을 제공하세요. manifest 갱신은 학생이 기본값을 맞추는 명령이 아니라 강사가 새 기준선을 배포하는 작업입니다. 기능 변경은 해당 설정·코드·SQL·테스트·PRD가 일치해야 합니다.

학생에게 파일명만 던지지 말고 하는 일을 설명하세요. 예: decide.ts는 후보 가능 여부에서 다음 업무 결과를 계산하는 파일입니다. SQL은 실제 저장과 중복 확정을 막는 규칙입니다.

코드 제작은 VS Code Claude Code Haiku 4.5에서 프롬프트로 진행합니다. 실제 실행 검증과 미실행 범위를 구분해서 보고하세요. 배포·도메인 연결은 별도이며 성공했다고 추정하지 마세요.
